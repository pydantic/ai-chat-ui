[project]
name = "ai-chat-ui-test-server"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "pydantic-ai-slim[ui]>=2",
    "uvicorn",
]

[tool.uv]
dev-dependencies = [
    "pydantic-ai-slim[anthropic,google,openai]>=2",
]
